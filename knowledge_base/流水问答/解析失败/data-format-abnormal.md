# 交易数据格式异常

交易金额包含非数字字符、日期格式不标准、币种符号混入金额列等情况会导致字段解析失败。系统会对每笔记录进行校验，异常记录标记为 `warning` 放入 issues 列表。

常见异常：
- 金额列包含非数字字符（如标点、空格、币种符号）
- 日期格式不标准（如中文年月日混用）
- 币种符号混入金额列（如 `¥1,234.56`、`$100.00`）
- 方向标识缺失或无法解析
